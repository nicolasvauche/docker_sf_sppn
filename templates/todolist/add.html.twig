{% extends 'base.html.twig' %}

{% block title %}Ajouter une Todolist{% endblock %}

{% block body %}
    <section>
        <h1>Nouvelle Todolist</h1>

        {{ form_start(form) }}
            <div class="form-group">
                <label for="{{ form.name.vars.id }}">{{ form.name.vars.label }}</label>
                {{ form_widget(form.name) }}
                {{ form_errors(form.name) }}
            </div>

            <button class="app-button" type="submit">Créer</button>
        {{ form_end(form) }}
    </section>
{% endblock %}
